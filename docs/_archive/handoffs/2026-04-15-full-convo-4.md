# Full Convo 4

- Date: `2026-04-15`
- Purpose: close the `Data Sources` surface and prepare the next session for real source validation work

## What Was Rebuilt

- `Foundation > Data Sources`
  - renamed and reframed from a messy `Source Registry` dump into a real source layer
  - page now clearly covers:
    - repo docs
    - Google Sheets
    - databases / apps
    - external APIs
    - workspace connectors
    - known missing sources
  - model is now separated into:
    - `Source Contract`
    - `Access / Connector`
    - `Presence State`
    - `Trust State`

## UI / UX Decisions Locked In

- Keep `Data Sources` as one canonical page.
  - Do not split sources, connectors, and trust into separate pages yet.
  - They are attributes of one source record, not separate truth systems.
- Default to scan-first behavior.
  - `How To Read This` is collapsed by default.
  - source-state groups are collapsed by default
  - individual source cards are collapsed by default
- Separate `Signed Off` from merely `Connected`.
  - final buckets are:
    - `Signed Off`
    - `Connected But Provisional`
    - `Needs Verification`
    - `Not Connected Yet`

## Current Build Focus Model

- The `Data Sources` page now has a `Current Build Focus` drawer.
- It is collapsed by default.
- It is no longer a guessed vague card.
- It now shows:
  - exact source/work item
  - exact workbook and tab when applicable
  - direct open actions
  - a short validation checklist
  - a `Focus on` selector so the current source task can be changed explicitly

## Current Source Focus At Stop Point

- Current default focus should now be:
  - `FOUNDATION-002`
  - `Owners Dashboard · ADMIN ONLY - Deal Data Entry`
- This is the real work item the user said they are actively finishing.

## Meaning Of The Current Step

The current work is not generic “source cleanup.”

It is:

- validate the `Owners Dashboard` source contract
- on the exact tab:
  - `ADMIN ONLY - Deal Data Entry`
- using the existing source note and live sheet
- then finalize the signed-off boundary in the system

The UI/checklist now reflects that more plainly.

## Bugs Fixed In This Pass

- fixed a real UI bug where `Current Build Focus` did not render
  - cause: reading `hub.backlog` instead of `hub.backlogItems`
- fixed a real sorting bug
  - `P0` and `executing` values could sort incorrectly because `0` was being treated like missing in JS fallback logic
- removed overly broad source-focus heuristics that were pulling in irrelevant cards like MarketMasters data work

## Source Docs / Notes Tightened

- `docs/source-registry.md`
  - renamed in-place as the operator note behind the live page
  - now clearly states that the page, not the markdown note, is the front door
- `FOUNDATION-002`
  - wording tightened so it explicitly says the current tab focus is `ADMIN ONLY - Deal Data Entry`

## Commits In This Pass

- `3ca02bb` — `Restructure data sources into a real source layer`
- `b334c6f` — `Collapse data sources for scan-first review`
- `fd1fef6` — `Add source build focus and signed-off split`
- `3919da9` — `Fix source build focus rendering`
- `24ef943` — `Add explicit source focus control and fix backlog sort`
- `acc0cc5` — `Clarify source focus with sheet actions and checklist`

## Where To Resume Tomorrow

Start in:

1. `Foundation > Data Sources`
2. Open `Current Build Focus`
3. Confirm the focus is still:
   - `Owners Dashboard · ADMIN ONLY - Deal Data Entry`
4. Use the open actions:
   - `Open Owners Dashboard`
   - `Open Deal Ledger Notes`
5. Finish the final validation / sign-off pass for `SRC-OWNERS-001`

## Recommended Next Step After That

After `SRC-OWNERS-001` is truly signed off:

1. update the source boundary in the system
2. move the source into the right trusted state
3. then move to:
   - `FOUNDATION-003`
   - `SRC-FINANCE-001`

